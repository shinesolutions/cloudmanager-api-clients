# Cloud Manager API
#
# This API allows access to Cloud Manager programs, pipelines, and environments by an authorized technical account created through the Adobe I/O Console. The base url for this API is https://cloudmanager.adobe.io, e.g. to get the list of programs for an organization, you would make a GET request to https://cloudmanager.adobe.io/api/programs (with the correct set of headers as described below). This swagger file can be downloaded from https://raw.githubusercontent.com/AdobeDocs/cloudmanager-api-docs/master/swagger-specs/api.yaml.
#
# The version of the OpenAPI document: 1.0.0
# Contact: opensource@shinesolutions.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PipelineListEmbedded
#' @description PipelineListEmbedded Class
#' @format An \code{R6Class} generator object
#' @field pipelines  list( \link{Pipeline} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PipelineListEmbedded <- R6::R6Class(
  'PipelineListEmbedded',
  public = list(
    `pipelines` = NULL,
    initialize = function(`pipelines`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`pipelines`)) {
        stopifnot(is.vector(`pipelines`), length(`pipelines`) != 0)
        sapply(`pipelines`, function(x) stopifnot(R6::is.R6(x)))
        self$`pipelines` <- `pipelines`
      }
    },
    toJSON = function() {
      PipelineListEmbeddedObject <- list()
      if (!is.null(self$`pipelines`)) {
        PipelineListEmbeddedObject[['pipelines']] <-
          lapply(self$`pipelines`, function(x) x$toJSON())
      }

      PipelineListEmbeddedObject
    },
    fromJSON = function(PipelineListEmbeddedJson) {
      PipelineListEmbeddedObject <- jsonlite::fromJSON(PipelineListEmbeddedJson)
      if (!is.null(PipelineListEmbeddedObject$`pipelines`)) {
        self$`pipelines` <- ApiClient$new()$deserializeObj(PipelineListEmbeddedObject$`pipelines`, "array[Pipeline]", loadNamespace("openapi"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`pipelines`)) {
        sprintf(
        '"pipelines":
        [%s]
',
        paste(sapply(self$`pipelines`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(PipelineListEmbeddedJson) {
      PipelineListEmbeddedObject <- jsonlite::fromJSON(PipelineListEmbeddedJson)
      self$`pipelines` <- ApiClient$new()$deserializeObj(PipelineListEmbeddedObject$`pipelines`, "array[Pipeline]", loadNamespace("openapi"))
      self
    }
  )
)
