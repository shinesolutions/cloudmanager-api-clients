# Cloud Manager API
#
# This API allows access to Cloud Manager programs, pipelines, and environments by an authorized technical account created through the Adobe I/O Console. The base url for this API is https://cloudmanager.adobe.io, e.g. to get the list of programs for an organization, you would make a GET request to https://cloudmanager.adobe.io/api/programs (with the correct set of headers as described below). This swagger file can be downloaded from https://raw.githubusercontent.com/AdobeDocs/cloudmanager-api-docs/master/swagger-specs/api.yaml.
#
# The version of the OpenAPI document: 1.0.0
# Contact: opensource@shinesolutions.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title EnvironmentListEmbedded
#' @description EnvironmentListEmbedded Class
#' @format An \code{R6Class} generator object
#' @field environments  list( \link{Environment} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
EnvironmentListEmbedded <- R6::R6Class(
  'EnvironmentListEmbedded',
  public = list(
    `environments` = NULL,
    initialize = function(`environments`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`environments`)) {
        stopifnot(is.vector(`environments`), length(`environments`) != 0)
        sapply(`environments`, function(x) stopifnot(R6::is.R6(x)))
        self$`environments` <- `environments`
      }
    },
    toJSON = function() {
      EnvironmentListEmbeddedObject <- list()
      if (!is.null(self$`environments`)) {
        EnvironmentListEmbeddedObject[['environments']] <-
          lapply(self$`environments`, function(x) x$toJSON())
      }

      EnvironmentListEmbeddedObject
    },
    fromJSON = function(EnvironmentListEmbeddedJson) {
      EnvironmentListEmbeddedObject <- jsonlite::fromJSON(EnvironmentListEmbeddedJson)
      if (!is.null(EnvironmentListEmbeddedObject$`environments`)) {
        self$`environments` <- ApiClient$new()$deserializeObj(EnvironmentListEmbeddedObject$`environments`, "array[Environment]", loadNamespace("openapi"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`environments`)) {
        sprintf(
        '"environments":
        [%s]
',
        paste(sapply(self$`environments`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(EnvironmentListEmbeddedJson) {
      EnvironmentListEmbeddedObject <- jsonlite::fromJSON(EnvironmentListEmbeddedJson)
      self$`environments` <- ApiClient$new()$deserializeObj(EnvironmentListEmbeddedObject$`environments`, "array[Environment]", loadNamespace("openapi"))
      self
    }
  )
)
