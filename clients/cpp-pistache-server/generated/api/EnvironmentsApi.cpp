/**
* Cloud Manager API
* This API allows access to Cloud Manager programs, pipelines, and environments by an authorized technical account created through the Adobe I/O Console. The base url for this API is https://cloudmanager.adobe.io, e.g. to get the list of programs for an organization, you would make a GET request to https://cloudmanager.adobe.io/api/programs (with the correct set of headers as described below). This swagger file can be downloaded from https://raw.githubusercontent.com/AdobeDocs/cloudmanager-api-docs/master/swagger-specs/api.yaml.
*
* The version of the OpenAPI document: 1.0.0
* Contact: opensource@shinesolutions.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "EnvironmentsApi.h"
#include "Helpers.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

EnvironmentsApi::EnvironmentsApi(std::shared_ptr<Pistache::Rest::Router> rtr) { 
    router = rtr;
}

void EnvironmentsApi::init() {
    setupRoutes();
}

void EnvironmentsApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Delete(*router, base + "/api/program/:programId/environment/:environmentId", Routes::bind(&EnvironmentsApi::delete_environment_handler, this));
    Routes::Get(*router, base + "/api/program/:programId/environment/:environmentId/logs/download", Routes::bind(&EnvironmentsApi::download_logs_handler, this));
    Routes::Get(*router, base + "/api/program/:programId/environment/:environmentId", Routes::bind(&EnvironmentsApi::get_environment_handler, this));
    Routes::Get(*router, base + "/api/program/:programId/environment/:environmentId/logs", Routes::bind(&EnvironmentsApi::get_environment_logs_handler, this));
    Routes::Get(*router, base + "/api/program/:programId/environments", Routes::bind(&EnvironmentsApi::get_environments_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&EnvironmentsApi::environments_api_default_handler, this));
}

void EnvironmentsApi::delete_environment_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    // Getting the path params
    auto programId = request.param(":programId").as<std::string>();
    auto environmentId = request.param(":environmentId").as<std::string>();
    
    // Getting the header params
    auto xGwImsOrgId = request.headers().tryGetRaw("x-gw-ims-org-id");
    auto authorization = request.headers().tryGetRaw("Authorization");
    auto xApiKey = request.headers().tryGetRaw("x-api-key");

    try {
      this->delete_environment(programId, environmentId, xGwImsOrgId, authorization, xApiKey, response);
    } catch (nlohmann::detail::exception &e) {
        //send a 400 error
        response.send(Pistache::Http::Code::Bad_Request, e.what());
        return;
    } catch (std::exception &e) {
        //send a 500 error
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
        return;
    }

}
void EnvironmentsApi::download_logs_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    // Getting the path params
    auto programId = request.param(":programId").as<std::string>();
    auto environmentId = request.param(":environmentId").as<std::string>();
    
    // Getting the query params
    auto serviceQuery = request.query().get("service");
    Pistache::Optional<std::string> service;
    if(!serviceQuery.isEmpty()){
        std::string valueQuery_instance;
        if(fromStringValue(serviceQuery.get(), valueQuery_instance)){
            service = Pistache::Some(valueQuery_instance);
        }
    }
    auto nameQuery = request.query().get("name");
    Pistache::Optional<std::string> name;
    if(!nameQuery.isEmpty()){
        std::string valueQuery_instance;
        if(fromStringValue(nameQuery.get(), valueQuery_instance)){
            name = Pistache::Some(valueQuery_instance);
        }
    }
    auto dateQuery = request.query().get("date");
    Pistache::Optional<std::string> date;
    if(!dateQuery.isEmpty()){
        std::string valueQuery_instance;
        if(fromStringValue(dateQuery.get(), valueQuery_instance)){
            date = Pistache::Some(valueQuery_instance);
        }
    }
    
    // Getting the header params
    auto xGwImsOrgId = request.headers().tryGetRaw("x-gw-ims-org-id");
    auto authorization = request.headers().tryGetRaw("Authorization");
    auto xApiKey = request.headers().tryGetRaw("x-api-key");
    auto accept = request.headers().tryGetRaw("Accept");

    try {
      this->download_logs(programId, environmentId, service, name, date, xGwImsOrgId, authorization, xApiKey, accept, response);
    } catch (nlohmann::detail::exception &e) {
        //send a 400 error
        response.send(Pistache::Http::Code::Bad_Request, e.what());
        return;
    } catch (std::exception &e) {
        //send a 500 error
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
        return;
    }

}
void EnvironmentsApi::get_environment_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    // Getting the path params
    auto programId = request.param(":programId").as<std::string>();
    auto environmentId = request.param(":environmentId").as<std::string>();
    
    // Getting the header params
    auto xGwImsOrgId = request.headers().tryGetRaw("x-gw-ims-org-id");
    auto authorization = request.headers().tryGetRaw("Authorization");
    auto xApiKey = request.headers().tryGetRaw("x-api-key");

    try {
      this->get_environment(programId, environmentId, xGwImsOrgId, authorization, xApiKey, response);
    } catch (nlohmann::detail::exception &e) {
        //send a 400 error
        response.send(Pistache::Http::Code::Bad_Request, e.what());
        return;
    } catch (std::exception &e) {
        //send a 500 error
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
        return;
    }

}
void EnvironmentsApi::get_environment_logs_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    // Getting the path params
    auto programId = request.param(":programId").as<std::string>();
    auto environmentId = request.param(":environmentId").as<std::string>();
    
    // Getting the query params
    auto serviceQuery = request.query().get("service");
    Pistache::Optional<std::vector<std::string>> service;
    if(!serviceQuery.isEmpty()){
        std::vector<std::string> valueQuery_instance;
        if(fromStringValue(serviceQuery.get(), valueQuery_instance)){
            service = Pistache::Some(valueQuery_instance);
        }
    }
    auto nameQuery = request.query().get("name");
    Pistache::Optional<std::vector<std::string>> name;
    if(!nameQuery.isEmpty()){
        std::vector<std::string> valueQuery_instance;
        if(fromStringValue(nameQuery.get(), valueQuery_instance)){
            name = Pistache::Some(valueQuery_instance);
        }
    }
    auto daysQuery = request.query().get("days");
    Pistache::Optional<int32_t> days;
    if(!daysQuery.isEmpty()){
        int32_t valueQuery_instance;
        if(fromStringValue(daysQuery.get(), valueQuery_instance)){
            days = Pistache::Some(valueQuery_instance);
        }
    }
    
    // Getting the header params
    auto xGwImsOrgId = request.headers().tryGetRaw("x-gw-ims-org-id");
    auto authorization = request.headers().tryGetRaw("Authorization");
    auto xApiKey = request.headers().tryGetRaw("x-api-key");

    try {
      this->get_environment_logs(programId, environmentId, days, xGwImsOrgId, authorization, xApiKey, service, name, response);
    } catch (nlohmann::detail::exception &e) {
        //send a 400 error
        response.send(Pistache::Http::Code::Bad_Request, e.what());
        return;
    } catch (std::exception &e) {
        //send a 500 error
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
        return;
    }

}
void EnvironmentsApi::get_environments_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    // Getting the path params
    auto programId = request.param(":programId").as<std::string>();
    
    // Getting the query params
    auto typeQuery = request.query().get("type");
    Pistache::Optional<std::string> type;
    if(!typeQuery.isEmpty()){
        std::string valueQuery_instance;
        if(fromStringValue(typeQuery.get(), valueQuery_instance)){
            type = Pistache::Some(valueQuery_instance);
        }
    }
    
    // Getting the header params
    auto xGwImsOrgId = request.headers().tryGetRaw("x-gw-ims-org-id");
    auto authorization = request.headers().tryGetRaw("Authorization");
    auto xApiKey = request.headers().tryGetRaw("x-api-key");

    try {
      this->get_environments(programId, xGwImsOrgId, authorization, xApiKey, type, response);
    } catch (nlohmann::detail::exception &e) {
        //send a 400 error
        response.send(Pistache::Http::Code::Bad_Request, e.what());
        return;
    } catch (std::exception &e) {
        //send a 500 error
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
        return;
    }

}

void EnvironmentsApi::environments_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

}
}
}
}

