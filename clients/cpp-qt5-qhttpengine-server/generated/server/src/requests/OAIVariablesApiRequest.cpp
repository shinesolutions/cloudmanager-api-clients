/**
 * Cloud Manager API
 * This API allows access to Cloud Manager programs, pipelines, and environments by an authorized technical account created through the Adobe I/O Console. The base url for this API is https://cloudmanager.adobe.io, e.g. to get the list of programs for an organization, you would make a GET request to https://cloudmanager.adobe.io/api/programs (with the correct set of headers as described below). This swagger file can be downloaded from https://raw.githubusercontent.com/AdobeDocs/cloudmanager-api-docs/master/swagger-specs/api.yaml.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: opensource@shinesolutions.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QVariantMap>
#include <QDebug>

#include "OAIHelpers.h"
#include "OAIVariablesApiRequest.h"

namespace OpenAPI {

OAIVariablesApiRequest::OAIVariablesApiRequest(QHttpEngine::Socket *s, QSharedPointer<OAIVariablesApiHandler> hdl) : QObject(s), socket(s), handler(hdl) {
    auto headers = s->headers();
    for(auto itr = headers.begin(); itr != headers.end(); itr++) {
        requestHeaders.insert(QString(itr.key()), QString(itr.value()));
    }
}

OAIVariablesApiRequest::~OAIVariablesApiRequest(){
    disconnect(this, nullptr, nullptr, nullptr);
    qDebug() << "OAIVariablesApiRequest::~OAIVariablesApiRequest()";
}

QMap<QString, QString>
OAIVariablesApiRequest::getRequestHeaders() const {
    return requestHeaders;
}

void OAIVariablesApiRequest::setResponseHeaders(const QMultiMap<QString, QString>& headers){
    for(auto itr = headers.begin(); itr != headers.end(); ++itr) {
        responseHeaders.insert(itr.key(), itr.value());
    }
}


QHttpEngine::Socket* OAIVariablesApiRequest::getRawSocket(){
    return socket;
}


void OAIVariablesApiRequest::getEnvironmentVariablesRequest(const QString& program_idstr, const QString& environment_idstr){
    qDebug() << "/api/program/{programId}/environment/{environmentId}/variables";
    connect(this, &OAIVariablesApiRequest::getEnvironmentVariables, handler.data(), &OAIVariablesApiHandler::getEnvironmentVariables);

    
    QString program_id;
    fromStringValue(program_idstr, program_id);
        QString environment_id;
    fromStringValue(environment_idstr, environment_id);
    
    QString x_gw_ims_org_id;
    if(socket->headers().keys().contains("x_gw_ims_org_id")){
        fromStringValue(socket->queryString().value("x_gw_ims_org_id"), x_gw_ims_org_id);
    }
    
    QString authorization;
    if(socket->headers().keys().contains("authorization")){
        fromStringValue(socket->queryString().value("authorization"), authorization);
    }
    
    QString x_api_key;
    if(socket->headers().keys().contains("x_api_key")){
        fromStringValue(socket->queryString().value("x_api_key"), x_api_key);
    }
    

    emit getEnvironmentVariables(program_id, environment_id, x_gw_ims_org_id, authorization, x_api_key);
}


void OAIVariablesApiRequest::patchEnvironmentVariablesRequest(const QString& program_idstr, const QString& environment_idstr){
    qDebug() << "/api/program/{programId}/environment/{environmentId}/variables";
    connect(this, &OAIVariablesApiRequest::patchEnvironmentVariables, handler.data(), &OAIVariablesApiHandler::patchEnvironmentVariables);

    
    QString program_id;
    fromStringValue(program_idstr, program_id);
        QString environment_id;
    fromStringValue(environment_idstr, environment_id);
    
    QString x_gw_ims_org_id;
    if(socket->headers().keys().contains("x_gw_ims_org_id")){
        fromStringValue(socket->queryString().value("x_gw_ims_org_id"), x_gw_ims_org_id);
    }
    
    QString authorization;
    if(socket->headers().keys().contains("authorization")){
        fromStringValue(socket->queryString().value("authorization"), authorization);
    }
    
    QString x_api_key;
    if(socket->headers().keys().contains("x_api_key")){
        fromStringValue(socket->queryString().value("x_api_key"), x_api_key);
    }
    
    QString content_type;
    if(socket->headers().keys().contains("content_type")){
        fromStringValue(socket->queryString().value("content_type"), content_type);
    }
     
    QJsonDocument doc;
    QList<OAIVariable> body;
    if(socket->readJson(doc)){
        QJsonArray jsonArray = doc.array();
        foreach(QJsonValue obj, jsonArray) {
            OAIVariable o;
            ::OpenAPI::fromJsonValue(o, obj);
            body.append(o);
        }
    }
    

    emit patchEnvironmentVariables(program_id, environment_id, x_gw_ims_org_id, authorization, x_api_key, content_type, body);
}



void OAIVariablesApiRequest::getEnvironmentVariablesResponse(const OAIVariableList& res){
    writeResponseHeaders();
    QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
    socket->writeJson(resDoc);
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIVariablesApiRequest::patchEnvironmentVariablesResponse(const OAIVariableList& res){
    writeResponseHeaders();
    QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
    socket->writeJson(resDoc);
    if(socket->isOpen()){
        socket->close();
    }
}


void OAIVariablesApiRequest::getEnvironmentVariablesError(const OAIVariableList& res, QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    writeResponseHeaders();
    Q_UNUSED(error_str);  // response will be used instead of error string
    QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
    socket->writeJson(resDoc);
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIVariablesApiRequest::patchEnvironmentVariablesError(const OAIVariableList& res, QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    writeResponseHeaders();
    Q_UNUSED(error_str);  // response will be used instead of error string
    QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
    socket->writeJson(resDoc);
    if(socket->isOpen()){
        socket->close();
    }
}


void OAIVariablesApiRequest::sendCustomResponse(QByteArray & res, QNetworkReply::NetworkError error_type){
    Q_UNUSED(error_type); // TODO
    socket->write(res);
    if(socket->isOpen()){
        socket->close();
    }    
}

void OAIVariablesApiRequest::sendCustomResponse(QIODevice *res, QNetworkReply::NetworkError error_type){
    Q_UNUSED(error_type);  // TODO
    socket->write(res->readAll());
    if(socket->isOpen()){
        socket->close();
    }
}

}
